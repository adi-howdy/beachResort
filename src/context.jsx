import React, { Component } from "react";

export const RoomContext = React.createContext();
//Provider is responsible for allowing all  component in component tree to access it.
// Consumer used to access those data

//export default RoomContext;

//export   default  { RoomConsumer,  RoomContext };

//{/**In class based component children are accessed as:{this.props.children}**/}
